version: 2
machine:
  node:
    version: 8.9.4
jobs:
  test:
    machine: true
    steps:
      - checkout
      - run: npm install
      - run: npm run coverage:report
  build:
    machine: true
    steps:
      - checkout
      - run: npm run build
