import oscStore from './oscReducer.js';

// Once we have more than one reducer
// import { combineReducers } from 'redux';
// const rootReducer = combineReducers({ eventsReducer, blah });

const rootReducer = oscStore;

export default rootReducer;
